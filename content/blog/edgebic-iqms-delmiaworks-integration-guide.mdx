---
title: "EDGEBIC + IQMS / DELMIAworks: The Complete Scheduling Integration Guide"
description: "IQMS DELMIAworks production scheduling integration with EDGEBIC step by step: export items, work centers, routings and orders, map columns once, schedule at finite capacity."
published: 2026-07-31T12:00:00.000Z
modified: 2026-07-31T12:00:00.000Z
category: ERP Integration (EDGEBIC)
author: user-solutions
cluster: edgebic-erp-integration
pillarSlug: edgebic-erp-integration-guide
keywords:
  - IQMS DELMIAworks production scheduling integration
  - DELMIAworks finite capacity scheduling
  - IQMS scheduling add-on
  - EDGEBIC DELMIAworks integration
  - DELMIAworks routing export scheduling
  - import mask ERP integration
  - injection molding finite capacity scheduling
targetPhrase: "IQMS DELMIAworks production scheduling integration"
wordCount: 2070
readingTime: 10
faqQuestions:
  - question: "Does EDGEBIC have a certified IQMS or DELMIAworks connector?"
    answer: "No, and that is a deliberate design choice. EDGEBIC integrates with DELMIAworks through reusable Excel, CSV, and database import masks rather than a certified connector. You map the columns of a DELMIAworks export once, and every later run is two clicks. Nothing is installed inside the ERP, no schema is extended, and no interface object enters its upgrade path. The same file-based method has fed schedules in this product line since 1991."
  - question: "What DELMIAworks data does EDGEBIC need to build a finite capacity schedule?"
    answer: "Four exports carry a complete scheduling model: items, work centers with machine counts, routings with operation sequence and run and setup times, and open orders with quantities and dates. A fifth optional export carries logged hours from the floor. Each one goes through its own import mask, and every row returns as Created, Updated, Reused, or Failed with a per-run log."
  - question: "Will adding EDGEBIC change how we run DELMIAworks MES on the floor?"
    answer: "No. DELMIAworks stays the system of record for real-time production monitoring, quality, inventory, and financials. EDGEBIC reads exports, builds a finite capacity schedule against real shifts and machine counts, and hands dates and dispatch lists back as Excel files. Your MES data collection keeps running exactly as it does today, so no floor process has to be redesigned."
  - question: "Does this fit a plastics or repetitive plant, not just a job shop?"
    answer: "Yes. The setup matrix models material and color changeovers directly, which is where a molding plant loses the most sequencing time, and machine instances model banks of identical presses so two runs on two presses schedule side by side. Setup families let you maintain color-family and material-family changeover rules once rather than as thousands of part-to-part pairs, which is exactly the shape of a plastics changeover problem."
qaQuestions:
  - question: "Our molding plant loses hours to color and material changeovers, and a single setup time per operation cannot express that going from white to black costs far more than the reverse. How does EDGEBIC handle that from a DELMIAworks export?"
    answer: "The changeover cost lives in a sequence-dependent setup matrix inside EDGEBIC, not in any export column, because no ERP field can express that the cost depends on what ran immediately before. You group parts into setup families (by color or by material) and set the changeover time between families once, so you maintain dozens of entries instead of thousands of part-to-part pairs. The optimizer then sequences compatible runs together and orders families to reduce total changeover, using mathematical optimization with a proven optimality gap plus a multi-run layer guaranteed never worse than the baseline. The DELMIAworks export supplies the parts, machines, and routings; the changeover intelligence is configured once in EDGEBIC and applies to every imported order afterward."
  - question: "We run banks of identical presses and DELMIAworks tracks them as a work center with a capacity number. Two identical presses, does modeling that separately really change the schedule?"
    answer: "It changes it by a factor. A work center modeled as one resource with a doubled capacity number can look right in a weekly total while being wrong every single day, because it will plan one run that occupies the whole cell when two runs could physically happen side by side. In the work center import you set the instance count to the number of identical presses, and the engine plans that many simultaneous runs, load balancing across them or dedicating one press per job per day when tooling changeovers make that the right rule. A work center group goes further and treats the press bank as a pool the engine re-shops on every reschedule, picking the press that finishes first."
---

**Integrating IQMS / DELMIAworks with EDGEBIC means exporting the items, work centers, routings, and open orders DELMIAworks already holds to Excel or CSV, mapping those columns once with a reusable import mask, and letting EDGEBIC schedule the work against finite capacity: real shift hours, real machine counts, and sequence-dependent setups.** Executable dates then export back for DELMIAworks and your customers. There is no middleware server, no interface object inside the ERP, and no certified connector to maintain through an upgrade.

EDGEBIC by User Solutions is the newest generation of a scheduling line that has integrated with manufacturing ERPs this way since 1991. Across 35+ years the same approach has fed schedules for the US Navy, GE, BAE Systems, and Cummins, and its lineage includes a Fourth Shift integration at Plastilite Corporation, a plastics manufacturer, that the ERP vendor itself recommended. This guide covers the DELMIAworks case (formerly IQMS EnterpriseIQ): what to export, how the masks work, what the engine does with the result, and what a realistic first two weeks looks like in a repetitive or process plant.

## Where DELMIAworks stops and detailed scheduling starts

DELMIAworks is strong at real-time manufacturing: MES data collection, quality, inventory, and financials in one system, which is exactly what a high-volume plant needs. That belongs there and should stay there. The gap plants report sits in the planning decision that runs ahead of the floor:

- Which of these open orders actually fits on the constraint press or line this week?
- If the rush order jumps the queue, which committed dates slip and by how much?
- Is the shared assembly cell loaded to 85 percent or 145 percent next Tuesday?

Those are finite capacity questions. Answering them means modeling shifts, machine counts, material and color changeover sequences, and operator skills against every open order at once, which is a different computational problem from monitoring a running line. That is why a dedicated scheduling layer beside the ERP is the standard pattern. The general argument is in [finite versus infinite capacity scheduling](/blog/finite-vs-infinite-capacity-scheduling), and the gap-by-gap version for this ERP is the companion post on [IQMS / DELMIAworks scheduling gaps and how EDGEBIC fills them](/blog/iqms-delmiaworks-scheduling-gaps-and-how-edgebic-fills-them). The wider category view is on [where ERP falls short on scheduling](/blog/where-erp-falls-short-scheduling).

## The integration in one table

The [EDGEBIC ERP integration architecture](/edgebic-erp-integration) is identical for every ERP, DELMIAworks included:

| Direction | Data | How it moves |
|---|---|---|
| DELMIAworks → EDGEBIC | Items | Excel or CSV export → Product import mask |
| DELMIAworks → EDGEBIC | Work centers | Export → Workcenter import mask |
| DELMIAworks → EDGEBIC | Routings / operations | Export → BOR (bill of routing) import mask, two-pass |
| DELMIAworks → EDGEBIC | Open orders | Export → SalesOrder import mask |
| DELMIAworks → EDGEBIC | Logged hours (optional) | Export → Actuals import mask |
| EDGEBIC → DELMIAworks | Executable start and end dates, dispatch lists | Excel export from Job View and reports |

An **import mask** is a saved recipe. It remembers which kind of data you are importing, what format it arrives in (an Excel workbook, or comma, semicolon, tab, or space delimited text), whether the first row carries headings, and how each column of your file maps onto an EDGEBIC field. You build it once by dragging your file's column headings onto EDGEBIC's target fields. Every run after that is two clicks: pick the mask, press Do It.

Every row in your file produces exactly one outcome: **Created**, **Updated**, **Reused** (found and deliberately left alone), or **Failed** (rejected, with the reason recorded). A result dialog shows the counts and a per-run log file records every row. Nothing half-imports silently, which matters when a governance team asks what a run actually changed.

## Step 1: the four exports

You do not need everything DELMIAworks knows. Four exports carry a complete scheduling model.

1. **Items.** The item number, description, unit of measure, and any cost or lead-time columns you want visible. The identifier is the natural key and matching is case-insensitive, so `CAP-BLK` in the file finds `Cap-Blk` in the database.
2. **Work centers.** The work center identifier, how many identical machines it holds (the press or line count), default setup hours, efficiency, and an hourly rate if you want cost rollups. Bottleneck and shift assignment flags can ride along in the same file.
3. **Routings.** One row per operation: end item, work center, sequence number, hours per unit, setup time, queue time.
4. **Open orders.** Item, quantity, order reference, and dates.

Any report, list, or query in your DELMIAworks environment that saves to Excel or CSV is a valid source. You are not writing code, not touching the database directly, and not asking for a custom interface. The file is the interface, which is exactly why nothing here enters your ERP's upgrade path.

## Step 2: map the columns once

Build one mask per file. Name it after the routine rather than the file: `Weekly DELMIAworks Orders` outlives `orders-week29.xlsx`. Mandatory fields are flagged in the mask editor, and the run refuses to start until they are mapped, so a half-built mask fails before it reads a single row rather than half-way through.

Two mask features carry most of the weight in a DELMIAworks integration:

**Unit conversion.** ERPs and schedulers disagree about units constantly. Set a conversion factor on any numeric column and the multiplication happens before the value is stored. A cycle time quoted in seconds becomes hours with `0.000278`, which is exactly the unit a molding cycle usually lives in. Minutes to hours is `0.016667`. A standard time quoted per lot of 100 pieces becomes per piece with `0.01`. Different columns in one file can carry different factors, so a routing export mixing per-piece cycle seconds with per-changeover setup minutes lands correctly in one run.

**Blank-cell preservation.** On an update run, a blank cell keeps the existing value instead of wiping it. A file carrying only item number and price refreshes prices and touches nothing else. Note the rule: a zero is a value, not a blank, so leave a column out rather than filling it with zeros you do not mean.

## Step 3: the routing import, and why it takes two passes

Routings are the hardest data to move between systems because operations reference each other: op 10 feeds op 20 feeds op 30. A naive row-by-row import cannot wire those links, because op 20 does not exist yet when op 10 is written.

EDGEBIC's routing import therefore runs in **two passes**. Pass one reads and validates every row, auto-creates any work center or item the file names that does not exist yet, and buffers the steps. Pass two groups the buffered rows by end item, sorts them by sequence number, writes them, and then wires the chain: 10 to 20, 20 to 30, and the terminal step to the finished item so the routing renders as one connected flow in the [graphical routing designer](/blog/edgebic-graphical-bor-designer-tour).

Three conventions save time later:

- **Number sequences in gaps of 10.** When engineering inserts a secondary op next quarter it becomes 25 and nothing renumbers.
- **Keep all of one item's steps in one file.** A routing import wipes and recreates that item's steps once per run, which is what makes re-imports idempotent. Splitting one item across two runs means the second run's wipe deletes the first run's steps.
- **Re-importing is safe for running orders.** Every scheduled job carries a frozen snapshot of the routing it was planned with, so a routing change applies to future jobs and leaves work in progress untouched.

## Step 4: orders in, then run the scheduler

The order import maps item, quantity, reference, and dates. When your export carries an order reference, rows sharing that reference group under one order and their jobs auto-number as `{Ref}-{line}`, which keeps a multi-line release together instead of scattering it.

One rule matters more than any other: **imports never schedule anything.** After the run, imported orders sit as unscheduled demand. You then run the scheduler, which states its scope before it plans (how many jobs are new and how many existing jobs are being rescheduled), and the new work slots around everything already committed on the floor. Data movement and planning stay separate on purpose, so an import can never silently rearrange a plant.

Once the data is in, the whole engine applies to your DELMIAworks work: finite capacity across multiple shifts and machine instances, [work center groups that re-shop the machine pool on every reschedule](/blog/edgebic-work-center-groups-explained), a [sequence-dependent setup matrix](/blog/edgebic-setup-matrix-explained) built around color and material families, lot streaming with transfer batches, operator skills, and mathematical optimization with a proven optimality gap. The [EDGEBIC product overview](/edgebic) maps the engine.

## Step 5: sending executable dates back

The return trip is Excel. The Job View grid exports the job schedule as a workbook with colored cells and a legend sheet. The work center schedule exports the same way, and every report dialog exports to Excel or PDF using the column layout you saved, which turns a dispatch list into a saved report rather than a document someone rebuilds each morning.

From there, revised dates go back into DELMIAworks through the order-maintenance path your team already uses. There is no automated write-back, which is deliberate: your ERP data stays under your team's control and inside your existing approval process. The category framing for this split is on the [ERP scheduling add-on page](/erp-scheduling-add-on).

## A realistic first two weeks

The method behind this guide has a documented benchmark in the User Solutions lineage: at Plastilite Corporation, a plastics manufacturer, the ERP vendor itself recommended User Solutions scheduling, and the team went from first export to a complete optimized schedule with ERP integration in 5 days. A larger repetitive estate usually wants a little more runway for cleanup rather than for the technical work.

| Days | Work |
|---|---|
| 1 to 2 | Export items and work centers; build and run those two masks; verify counts against DELMIAworks |
| 3 to 4 | Export routings; build the routing mask with cycle-time conversions; import and review the routing designer |
| 5 | Export open orders; import; run the first full finite capacity schedule |
| 6 to 8 | Compare EDGEBIC dates to current commitments; correct instance counts, shifts, and changeover families where reality disagrees |
| 9 to 10 | Lock the weekly rhythm: saved masks, import order, scheduler run, exports back |

If you are starting from an empty database, the [green-field setup walkthrough](/blog/edgebic-greenfield-setup-walkthrough) covers the sequence in detail, and the [IQMS / DELMIAworks to EDGEBIC data mapping reference](/blog/iqms-delmiaworks-to-edgebic-data-mapping-reference) is the field-level lookup you keep open while building masks.

## Prove it with your own export

The fastest evaluation is not a feature list, it is your own file. Export this week's open orders and a routing file, then [bring them to a demo](/contact-us). Mapping them live takes minutes, and you leave having watched your own plant scheduled against its own capacity, changeover families included. Plants comparing several ERPs at once can also read the [Plex](/blog/edgebic-plex-integration-guide) and [SAP](/blog/edgebic-sap-integration-guide) versions of this guide: the method is identical, which is the point.

<Accordion title="Does EDGEBIC have a certified IQMS or DELMIAworks connector?">
No, and that is a deliberate design choice. EDGEBIC integrates with DELMIAworks through reusable Excel, CSV, and database import masks rather than a certified connector. You map the columns of a DELMIAworks export once, and every later run is two clicks. Nothing is installed inside the ERP, no schema is extended, and no interface object enters its upgrade path. The same file-based method has fed schedules in this product line since 1991.
</Accordion>

<Accordion title="What DELMIAworks data does EDGEBIC need to build a finite capacity schedule?">
Four exports carry a complete scheduling model: items, work centers with machine counts, routings with operation sequence and run and setup times, and open orders with quantities and dates. A fifth optional export carries logged hours from the floor. Each one goes through its own import mask, and every row returns as Created, Updated, Reused, or Failed with a per-run log.
</Accordion>

<Accordion title="Will adding EDGEBIC change how we run DELMIAworks MES on the floor?">
No. DELMIAworks stays the system of record for real-time production monitoring, quality, inventory, and financials. EDGEBIC reads exports, builds a finite capacity schedule against real shifts and machine counts, and hands dates and dispatch lists back as Excel files. Your MES data collection keeps running exactly as it does today, so no floor process has to be redesigned.
</Accordion>

<Accordion title="Does this fit a plastics or repetitive plant, not just a job shop?">
Yes. The setup matrix models material and color changeovers directly, which is where a molding plant loses the most sequencing time, and machine instances model banks of identical presses so two runs on two presses schedule side by side. Setup families let you maintain color-family and material-family changeover rules once rather than as thousands of part-to-part pairs, which is exactly the shape of a plastics changeover problem.
</Accordion>
