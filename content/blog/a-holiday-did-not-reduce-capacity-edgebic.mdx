---
title: "A Holiday Did Not Reduce Capacity: Causes and Fixes"
description: "When a holiday you added leaves capacity untouched, the cause is usually a holiday added after the last run, the wrong holiday level, or an override holding the day open. How to tell."
published: 2026-07-27T12:00:00.000Z
modified: 2026-08-10T12:00:00.000Z
category: Troubleshooting
author: user-solutions
cluster: edgebic-troubleshooting
pillarSlug: edgebic-troubleshooting-guide
keywords:
  - holiday did not reduce capacity
  - holiday not blocking work
  - work scheduled on holiday
  - plant holiday vs work center holiday
  - capacity override holiday
  - reschedule after adding holiday
targetPhrase: "holiday did not reduce capacity"
wordCount: 1403
readingTime: 7
faqQuestions:
  - question: "Why did adding a holiday not reduce my work center's capacity?"
    answer: "The most common reason is that the holiday was added after the last scheduling run, so the existing schedule still reflects the pre-holiday capacity until you reschedule. The other causes are the holiday being inactive or partial-day, a recurring holiday configured with the wrong month and day, a daily capacity override holding the day open regardless of the holiday, and reaching for the wrong tool entirely: anything narrower than a plant-wide closure is a per-day capacity override rather than a holiday record, because the current release has no per-shift or per-machine holiday screen. Reschedule after adding, then confirm the flags."
  - question: "What is the difference between a plant holiday, a shift holiday, and a work-center holiday?"
    answer: "A plant holiday closes every work center and every shift for that day, and it is the only closure record you can create in the current release. The engine also recognizes shift-scoped and work-center-scoped closures, but with no screen to enter them, anything narrower than the plant is expressed as a per-day capacity override of zero on the affected work center, shift, and date. So if you expected a narrow closure and the plant went dark, you used a plant holiday where an override was wanted; if you expected a plant-wide closure and most machines kept running, check whether you only zeroed a few overrides."
  - question: "Can a capacity override keep a day open even after I add a holiday?"
    answer: "Yes. A daily capacity override sets the available hours for a specific machine, shift, and date directly, and it takes priority over the shift formula and holidays. So an override left in place, such as an approved overtime day, keeps that cell open even when a holiday would otherwise close it. If a holiday is not reducing one specific cell, check for a daily override on that machine, shift, and date and reset it to the shift default."
qaQuestions:
  - question: "I added our plant shutdown day but the schedule still books work on it. What did I miss?"
    answer: "Almost certainly a reschedule. Adding a holiday changes the calendar, but the existing plan was built before the holiday existed, so it still shows work on that day until you rerun scheduling. Reschedule and the engine will skip the holiday and move that work off the day. If work still lands there after a reschedule, confirm the holiday is marked active and applies to the whole day rather than a narrow window, and check that no capacity override is holding a cell open, since an override wins over the shift formula for the day it covers."
  - question: "A recurring holiday reduces capacity in some years but not others. How is that possible?"
    answer: "A recurring holiday fires on the month and day of its reference date every year, and only the month and day matter, so a reference date with the wrong month or day fires on the wrong calendar date each year. If it looks right some years and wrong in others, more likely the specific date you are checking has a daily capacity override on it, or the holiday's active flag differs. Check the reference month and day, the active flag, and any override on the dates that are not reducing."
---

**When a holiday you added leaves capacity untouched, the cause is almost always a holiday added after the last scheduling run, the wrong holiday level, or a capacity override holding the day open.** [EDGEBIC by User Solutions](/edgebic) blocks a day from scheduling when the calendar says the work center is closed, but the existing plan reflects the calendar as it was at the last run, and holidays come in three scopes that block different things.

The controls you use to diagnose this are the holiday record, the anomaly report, and the capacity view. This post is the detailed version of the holiday-capacity symptom in the [EDGEBIC troubleshooting guide](/blog/edgebic-troubleshooting-guide). For the inverse symptom, where a job landed on a holiday, read [job scheduled on a holiday](/blog/job-scheduled-on-a-holiday-edgebic). For setup, [how to add a plant holiday](/blog/how-to-add-a-plant-holiday-in-edgebic) covers the plant level.

## The Three Closure Levels Block Different Things

EDGEBIC's engine resolves closures at three scopes, and knowing which one you actually used is half the diagnosis.

| Scope | Blocks | How you create it today |
|---|---|---|
| Plant | Every work center, every shift | The plant holidays screen |
| Shift | One shift, machines that run it | Per-day capacity overrides of zero on that shift |
| Work center | One machine | A per-day capacity override of zero on that machine |

Two consequences for this symptom. A partial-day plant holiday reduces only the hours where its window overlaps each shift, not the whole day, so a small window looks like almost no effect and is working correctly. And because the narrower scopes are overrides rather than holiday records, "my holiday did not reduce capacity" is sometimes really "I was looking for a holiday record that was never the right tool." [The calendar setup guide](/blog/how-to-set-up-shifts-holidays-downtime-in-edgebic) covers both routes.

## Cause 1: The Holiday Was Added After the Last Run

The existing schedule was built against the calendar as it stood at the last scheduling run. Adding a holiday afterward changes the calendar but does not touch the already-committed plan, so work stays booked on the holiday until you reschedule. This is the single most common reason a holiday appears to do nothing.

**How to tell:** the anomaly report flags bookings that fall on a holiday, and the presence of those rows on the new holiday's date confirms the plan predates the holiday. If you have not rescheduled since adding it, this is the cause.

**Fix:** reschedule the affected jobs. The engine skips the holiday and moves the work off the day. If work still lands there after a clean reschedule, check the level and flags below.

## Cause 2: The Closure Is Narrower Than You Intended

If you zeroed one machine's capacity expecting a plant-wide closure, the narrow reach explains why the rest of the plant still shows hours. An override closes one work center on one shift on one date and nothing else, so a plant with six machines running two shifts needs twelve entries to go fully dark, which is exactly why the plant holiday exists.

**How to tell:** open the per-day capacity view for a machine that is still showing hours on the date. If it carries no overridden flag, nothing closed it.

**Fix:** if you meant to close the whole plant, delete the overrides and add a single plant holiday instead. If you genuinely meant one machine or one crew, the rest of the plant staying open is correct behavior, and the thing to check is whether you covered every affected work center and both of its shifts. [Blocking a machine for a maintenance day](/blog/how-to-block-a-machine-for-a-maintenance-day-in-edgebic) covers the single-machine case and [closing one shift](/blog/how-to-close-one-shift-for-a-holiday-in-edgebic) covers the crew case.

## Cause 3: The Holiday Is Inactive or Partial-Day

A holiday only takes effect when it is marked active, and a partial-day holiday reduces only the overlapping hours rather than closing the day. An inactive holiday is ignored entirely, and a partial-day holiday set to a small window trims only a little capacity, which can look like no effect.

**How to tell:** read the active flag and the whole-day flag on the holiday. An inactive holiday does nothing; a partial-day one reduces only its window's overlap with the shift.

**Fix:** activate the holiday, and set it to apply to the whole day if you intended a full closure. If a partial holiday is correct, the reduced-but-not-zero capacity is the expected result, and if that reduction looks too large or too small, [a partial holiday that cut too much or too little capacity](/blog/my-partial-holiday-reduced-too-much-or-too-little-capacity-edgebic) walks the arithmetic behind it. [Scheduling a half-day holiday](/blog/how-to-schedule-a-half-day-holiday-in-edgebic) covers the partial case.

## Cause 4: A Capacity Override Is Holding the Day Open

A daily capacity override sets the available hours for a specific machine, shift, and date directly, and it takes priority over the shift formula and over holidays. An override left in place, such as an approved overtime day, keeps that cell open even when a holiday would otherwise close it.

**How to tell:** open the capacity view for the machine and date. An override shows the day carrying hours in a distinct color, and it wins over the holiday.

**Fix:** if the day should be closed, reset the cell to the shift default, which removes the override, then reschedule. If the override was deliberate, the open day is intended and the holiday is correctly deferring to the planner's chosen hours. [Overriding capacity for one day](/blog/how-to-override-capacity-for-one-day-in-edgebic) covers the override, and [reading a red day on the capacity view](/blog/reading-a-red-day-on-the-edgebic-capacity-view) shows how closed and overridden days appear.

## A Note on Recurring Holidays

A recurring holiday fires on the month and day of its reference date every year, and only the month and day matter; the year is ignored. A reference date with the wrong month or day fires on the wrong calendar date every year, so a recurring holiday that reduces capacity on the wrong day, or not on the day you expect, usually has a misconfigured reference date.

## How to Diagnose an Ineffective Holiday, in Order

1. **Reschedule the affected jobs.** A holiday added after the last run does nothing to the existing plan until you rerun.
2. **Check you used the right tool for the reach.** A plant holiday for the whole site; per-day overrides for one shift or one machine.
3. **Check the active and whole-day flags.** Inactive does nothing; partial-day trims only the overlap.
4. **Check for a daily capacity override** on any cell the holiday should close.
5. **For a recurring holiday, verify the reference month and day**, since only those two fields drive the recurrence.

## Prevention

- **Reschedule after any calendar change.** The plan reflects the calendar at the last run, so a new holiday only takes effect on the next reschedule.
- **Match the tool to the reach.** A plant shutdown is a plant holiday; a single machine's teardown is a per-day capacity override of zero; picking deliberately avoids both the too-wide and the too-narrow surprise.
- **Clear stale overrides before a closure.** An old overtime override silently holds a day open against a holiday, so review overrides on the dates you close.
- **Run the anomaly report after adding holidays.** The off-calendar checks flag any booking that survived on a holiday, so you catch a missed reschedule or a narrow scope before the floor does. If a whole machine shows no capacity when it should have some, [capacity shows zero for a work center](/blog/capacity-shows-zero-for-a-work-center-edgebic) covers that, and [shift and calendar mistakes](/blog/shift-and-calendar-mistakes-edgebic) covers the common calendar slips.

<Accordion title="Why did adding a holiday not reduce my work center's capacity?">
The most common reason is that the holiday was added after the last scheduling run, so the existing schedule still reflects the pre-holiday capacity until you reschedule. The other causes are the holiday being inactive or partial-day, a recurring holiday configured with the wrong month and day, a daily capacity override holding the day open regardless of the holiday, and reaching for the wrong tool entirely: anything narrower than a plant-wide closure is a per-day capacity override rather than a holiday record, because the current release has no per-shift or per-machine holiday screen. Reschedule after adding, then confirm the flags.
</Accordion>

<Accordion title="What is the difference between a plant holiday, a shift holiday, and a work-center holiday?">
A plant holiday closes every work center and every shift for that day, and it is the only closure record you can create in the current release. The engine also recognizes shift-scoped and work-center-scoped closures, but with no screen to enter them, anything narrower than the plant is expressed as a per-day capacity override of zero on the affected work center, shift, and date. So if you expected a narrow closure and the plant went dark, you used a plant holiday where an override was wanted; if you expected a plant-wide closure and most machines kept running, check whether you only zeroed a few overrides.
</Accordion>

<Accordion title="Can a capacity override keep a day open even after I add a holiday?">
Yes. A daily capacity override sets the available hours for a specific machine, shift, and date directly, and it takes priority over the shift formula and holidays. So an override left in place, such as an approved overtime day, keeps that cell open even when a holiday would otherwise close it. If a holiday is not reducing one specific cell, check for a daily override on that machine, shift, and date and reset it to the shift default.
</Accordion>
