---
title: "What Is a Continuous-Process Work Center?"
description: "A continuous-process work center runs flowing product you cannot count in transfer batches. Learn how the flag changes which lot-streaming model a schedule applies."
published: 2026-07-31T12:00:00.000Z
modified: 2026-07-31T12:00:00.000Z
category: Glossary (EDGEBIC)
author: user-solutions
cluster: edgebic-glossary
keywords:
  - continuous process work center
  - continuous process flag
  - flow production scheduling
  - lot streaming model
  - transfer batch ignored
  - discrete vs continuous process
targetPhrase: "continuous-process work center"
wordCount: 1010
readingTime: 5
faqQuestions:
  - question: "What is a continuous-process work center?"
    answer: "A continuous-process work center produces flowing product that cannot be counted in discrete transfer batches, such as a chemical reactor, an extruder, or a filling line handling liquid. When a work center is flagged continuous-process, the schedule always overlaps its steps using a start-to-start lag model rather than a piece-count transfer batch, because counting trays of parts makes no sense for a continuous flow."
  - question: "Why does a transfer batch size get ignored on a continuous-process work center?"
    answer: "Because the piece-count lot-streaming model does not apply to flowing product. On a continuous-process work center the engine always uses the start-to-start lag model, so any transfer batch size entered on a step routed there is deliberately ignored. The flag on the work center wins, which prevents a planner from accidentally applying a discrete-parts overlap rule to a process that has no discrete pieces to move."
  - question: "How do I know whether to flag a work center as continuous-process?"
    answer: "Flag it when the machine physically processes a continuous flow rather than discrete countable parts. A reactor, an oven with continuous throughput, an extrusion line, or a liquid filling stage are continuous-process. A mill, a press, or an assembly station handling separate pieces are not. The test is simple: if you cannot move a tray of finished units to the next step, the work center is continuous-process."
qaQuestions:
  - question: "I set a transfer batch on a step and the overlap did not change. The step runs on our extruder. What happened?"
    answer: "The extruder is almost certainly flagged continuous-process, and on a continuous-process work center the transfer batch is ignored by design. The engine uses the start-to-start lag model there instead, so the downstream step overlaps based on a time lag rather than a count of finished pieces. That is correct behavior, not a bug: you cannot ship a tray of extruded flow to the next station. If you want a specific overlap, set the lag rather than the batch on that step."
  - question: "Half our plant is discrete machining and half is continuous mixing. Do I have to choose one model for the whole shop?"
    answer: "No. The continuous-process flag lives on each work center, so your machining centers stay discrete and use piece-count transfer batches, while your mixing and reactor stages are flagged continuous-process and use the start-to-start lag model. A single routing can pass through both kinds of work center, and each step is overlapped by the model that fits the machine it runs on. You set the flag once per machine and the schedule handles the rest."
---

**A continuous-process work center produces flowing product that cannot be counted in discrete transfer batches, so the schedule overlaps its steps with a time-lag model instead of a piece count.** Reactors, extruders, ovens with continuous throughput, and liquid filling stages all fit this description: there is no tray of finished parts to hand to the next step, only a continuous flow. Flagging a work center as continuous-process tells the engine which lot-streaming model to apply. EDGEBIC by User Solutions reads the flag on every work center so process stages and discrete-parts stages each overlap the way their physics allows.

## How it works

Lot streaming lets a downstream step start before the upstream step finishes, overlapping the two to shorten the total run. There are two ways to express that overlap. The piece-count model moves a defined number of finished parts, a transfer batch, to the next step as soon as they exist. The start-to-start lag model overlaps the steps by a time offset instead, letting the successor begin a set amount of time after the predecessor starts.

The piece-count model only makes sense for discrete parts. You cannot move a "batch of 25" of a flowing liquid or a continuous extrusion. So when a work center is flagged continuous-process, the engine always uses the start-to-start lag model on steps routed there, and it ignores any transfer batch size that happens to be set on those steps. The work center's flag wins over the step's batch, which is the safeguard that keeps a discrete-parts rule from being applied to a process that has no discrete pieces.

The flag is a property of the machine, not the product or the routing, because the machine's physical character does not change from job to job. A reactor is continuous by nature; a mill is discrete by nature. Setting the flag once per work center is all it takes.

## A concrete example

Think about serving food two different ways. At a bakery, finished loaves come out in countable units, so you can carry a tray of 25 to the packing table as soon as those 25 are baked. That is piece-count streaming: real, countable parts moving downstream.

Now think about a juice line. The juice flows continuously through pasteurizing and into filling; there is no tray of finished juice to carry, only a continuous stream. You overlap those stages by starting the filling a set time after pasteurizing begins, not by counting units. That is the start-to-start lag model, and the juice line is a continuous-process work center.

In the plant, a chemical reactor that continuously processes product is continuous-process: you cannot count transfer batches of liquid, so the engine overlaps it by lag. A machining center feeding an assembly station is discrete, so it overlaps by moving trays of finished parts.

## How EDGEBIC uses it

The continuous-process flag is set on each work center. The step-by-step is in [how to mark a work center as continuous-process in EDGEBIC](/blog/how-to-mark-a-work-center-as-continuous-process-in-edgebic), and the wider picture of when and why to use it is in [continuous-process work centers](/blog/continuous-process-work-centers-edgebic).

Because the flag selects the lot-streaming model, it interacts directly with the piece-count settings. A [transfer batch](/blog/what-is-a-transfer-batch) entered on a step routed to a continuous-process work center is ignored on purpose, which is the exact scenario explained in [my transfer batch size was ignored](/blog/my-transfer-batch-size-was-ignored-edgebic). The full overlap mechanism, both models side by side, is covered in [EDGEBIC lot streaming](/blog/edgebic-lot-streaming-explained).

The flat handling lag that can sit on top of either model is the companion term [transfer delay](/blog/what-is-transfer-delay-in-lot-streaming). For the wider vocabulary, see the [manufacturing glossary](/blog/manufacturing-glossary), and to see process and discrete stages in one plan, explore [EDGEBIC](/edgebic).
