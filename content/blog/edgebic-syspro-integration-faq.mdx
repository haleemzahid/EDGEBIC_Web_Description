---
title: "EDGEBIC + SYSPRO Integration FAQ: The Questions Planners Ask First"
description: "Answers to the SYSPRO scheduling integration questions manufacturers ask first: exports, no connector, multi-site calendars, WIP jobs, and dates back to the ERP."
published: 2026-08-02T12:00:00.000Z
modified: 2026-08-02T12:00:00.000Z
category: ERP Integration (EDGEBIC)
author: user-solutions
cluster: edgebic-erp-integration
keywords:
  - SYSPRO scheduling integration questions
  - SYSPRO EDGEBIC FAQ
  - SYSPRO scheduling add-on questions
  - SYSPRO WIP job scheduling
  - SYSPRO finite capacity scheduling
  - SYSPRO production scheduling integration
targetPhrase: "SYSPRO scheduling integration questions"
wordCount: 1670
readingTime: 8
faqQuestions:
  - question: "Does EDGEBIC use a SYSPRO connector or e.net service?"
    answer: "No. EDGEBIC reads the Excel, CSV, or database exports SYSPRO already produces through reusable import masks. There is no e.net service to build against, no add-on deployed inside SYSPRO, and no integration credential to create or rotate. You map an export's columns onto EDGEBIC fields once and save the mask; every run after that is two clicks with a per-run log recording each row."
  - question: "Do imported WIP jobs schedule themselves?"
    answer: "No. An import changes data; the scheduler changes the plan. Imported WIP jobs appear as unscheduled demand and stay there until you run the scheduler, which states how many jobs are new and how many existing ones are being rescheduled before it plans anything. That separation means an import can never silently rearrange the floor."
  - question: "How do finite capacity dates get back into SYSPRO?"
    answer: "Through Excel and the WIP job date-maintenance path your process already uses. The Job View grid exports the job schedule as a workbook with colored cells and a legend sheet, and every report dialog exports to Excel or PDF using the column layout you saved. There is no automated write-back, which keeps your ERP records under your team's control."
qaQuestions:
  - question: "We run SYSPRO across three plants. Do we need three copies of the scheduler, or can one schedule cover all of them?"
    answer: "One scheduling database can hold work centers from more than one plant, each assigned to its own shift and holiday calendar, so a single schedule can span all three sites while still respecting each plant's real hours. The practical setup is to export per site or add a site column you filter within EDGEBIC, then assign each site's work centers to the correct shift patterns. Where a job moves between plants, the routing carries transit days so the calendar delay is explicit rather than hidden. You would only run separate scheduling databases if the plants are managed as genuinely independent operations that never share a plan."
  - question: "Our BOM and operations data sits in SYSPRO tables. Is a report export the only way in, or can EDGEBIC read the database?"
    answer: "A mask can read a database source directly, so a view over the SYSPRO tables works as a source in exactly the same way a saved Excel file does. Many sites settle on a small set of views, one per entity, that present the columns EDGEBIC needs, then a mask per view. The morning routine becomes pick the mask, press Do It, read the counts. The advantage over a report export is that the column names on a view stay stable, so the mapping never drifts. Either approach is valid, and you can mix them, using a view for master data and a saved report for the weekly job list."
---

**These are the questions SYSPRO manufacturers ask in the first conversation, answered without hedging: there is no connector, the interface is your existing exports or a database view, nothing is installed in SYSPRO, and nothing you import can disturb a job already running.** Each answer stands on its own, so skip to whichever one you came for.

EDGEBIC by User Solutions is a finite capacity scheduling platform that sits beside your ERP. The full walkthrough is the [complete SYSPRO integration guide](/blog/edgebic-syspro-integration-guide), and the field-level detail is in the [SYSPRO to EDGEBIC mapping reference](/blog/syspro-to-edgebic-data-mapping-reference).

## Does EDGEBIC connect through a SYSPRO service?

No, and that is deliberate. EDGEBIC integrates through reusable import masks that read Excel, CSV, delimited text, or a database source. You map your export's columns onto EDGEBIC's fields once and save the mask; every later run is two clicks. Because there is no service integration, there is no add-on in SYSPRO, no integration credential, no schema change to carry through an upgrade, and nothing to version-match when either side updates. A connector is a dependency. A file or a view is not.

## Does EDGEBIC replace SYSPRO?

No. SYSPRO stays the system of record for orders, inventory, purchasing, WIP, and financials. EDGEBIC reads exports, builds a finite capacity schedule against real shift hours and real machine counts, and hands back dates and dispatch lists as Excel files. Nothing about how your team uses SYSPRO changes. The general case for the split is in [why ERP needs a scheduling add-on](/erp-scheduling-add-on).

## What exactly has to come out of SYSPRO?

Four exports carry a complete scheduling model, and a fifth is optional. Stock codes (a code is the only mandatory column). Work centers (an identifier, plus capacity detail). BOM operations (end product, step name, an operation flag, and hours per unit). Open WIP jobs (item, quantity, and a date). Optionally, labor (job number, work center, date, and hours or pieces). Any report that saves to Excel or CSV is a valid source, and a database view works equally well.

## We run multiple SYSPRO sites. Can one schedule cover them?

Yes. A single scheduling database can hold work centers from more than one site, each assigned to its own shift and holiday calendar, so the plan respects each site's real hours rather than an average. Export per site or add a site column you filter within EDGEBIC, and carry inter-site transport as transit days on the routing so a step that ships between plants shows the calendar delay explicitly. You would only split into separate scheduling databases if the sites never share a plan.

## Our times come out in minutes. Do we convert them by hand?

No. Set a conversion factor of `0.016667` on the setup or run column in the operations mask and a 30-minute cell lands as 0.5 hours. Seconds use `0.000278`, and a time quoted per 100 pieces uses `0.01`. The factor is stored on the mask, so every future import converts identically without anyone remembering to do it, and the per-run log records every row if you want to audit the math.

## How long until the first real schedule?

Most shops see a first finite capacity schedule from their own data in the first working session, and a tuned one within the week. The documented benchmark in the User Solutions lineage is the Plastilite integration on Fourth Shift: 5 days, Monday to Friday, from first export to a complete optimized schedule with dates synchronized back to the ERP. That figure belongs to that case rather than being a promise, but the method is what repeats: export, map, import, schedule, tune.

## Do we have to clean our BOMs first?

No, and the attempt usually costs a month. Import what SYSPRO holds today, schedule it, and compare the dates with what your planner believes. The disagreements are your defect list and they arrive specific: a wrong hours-per-unit shows up as a job finishing implausibly early, a missing setup time shows up as a machine that changes over for free. Because a routing re-import wipes and recreates that item's steps, each corrected file replaces the last cleanly.

## Do imported WIP jobs schedule themselves?

No, and the separation is deliberate. An import changes data; the scheduler changes the plan. Imported jobs appear as unscheduled demand until you run the scheduler, and before the run starts a confirmation states the scope in plain numbers: how many jobs are new and how many existing jobs are being rescheduled. You always know the blast radius before anything moves.

## Can an import disturb jobs already running?

No, for two independent reasons. Every scheduled job carries a frozen snapshot of the routing it was planned with, so a routing re-import affects future jobs and leaves work in progress alone. And recorded work is never moved by any run: an operation with an actual start and an actual end is historical fact, and no reschedule, mode, or setting will shift it.

## We have several identical machines in one work center. How does EDGEBIC know that?

Through the machine-count column on the work center file, which most ERP exports leave out because a work center is usually modeled as one resource with a capacity figure. Fill the instance count deliberately for every multi-machine center: a cell of four machines imported as one produces a plan roughly four times too long, and it fails quietly, since nothing errors and the dates are simply wrong. The arithmetic is in [how EDGEBIC calculates work center capacity](/blog/how-edgebic-calculates-work-center-capacity).

## Can we automate the export and the import?

Partly, and it is worth being precise about which half. The export side automates well: a scheduled report or a database view that presents stable columns removes the only manual step that ever gets forgotten. The import side stays a deliberate act, because the result dialog exists to be read. A run that reports 40 unexpected Failed rows is information a person needs to see before the schedule is built on top of it. Most shops settle on automated exports plus a two-minute human import.

## What can import never bring across?

The settings that most change what a schedule looks like, because no ERP export has a column for them. Work center groups (machine pools re-shopped on every reschedule, with per-member efficiency factors). The sequence-dependent setup matrix that lets the optimizer sequence work to cut changeover. Operator skills and rosters. Bottleneck anchoring. Lot streaming with transfer batches. Each is configured once inside EDGEBIC and then applies to every imported job afterwards. The [EDGEBIC product overview](/edgebic) maps them and the [ERP integration hub](/edgebic-erp-integration) explains where the import boundary sits.

## Who runs this day to day?

The planner, in about twenty minutes each morning, without IT. Involve IT exactly once, to make the four exports saved reports or database views with stable columns. After that the routine is export, run the saved masks, run the scheduler, publish the dispatch lists. The same questions for other platforms are answered in the [Acumatica](/blog/edgebic-acumatica-integration-faq) and [MIE Trak Pro](/blog/edgebic-mie-trak-pro-integration-faq) FAQs, and the wider category framing is on the [ERP integration hub](/edgebic-erp-integration).

## Still deciding?

The fastest way to settle any of this is your own export. Pull this week's open WIP jobs and a BOM/operations file, then [bring them to a demo](/contact-us): mapping them live takes minutes, and you leave having watched your own shop scheduled against its own capacity.

<Accordion title="Does EDGEBIC use a SYSPRO connector or e.net service?">
No. EDGEBIC reads the Excel, CSV, or database exports SYSPRO already produces through reusable import masks. There is no e.net service to build against, no add-on deployed inside SYSPRO, and no integration credential to create or rotate. You map an export's columns onto EDGEBIC fields once and save the mask; every run after that is two clicks with a per-run log recording each row.
</Accordion>

<Accordion title="Do imported WIP jobs schedule themselves?">
No. An import changes data; the scheduler changes the plan. Imported WIP jobs appear as unscheduled demand and stay there until you run the scheduler, which states how many jobs are new and how many existing ones are being rescheduled before it plans anything. That separation means an import can never silently rearrange the floor.
</Accordion>

<Accordion title="How do finite capacity dates get back into SYSPRO?">
Through Excel and the WIP job date-maintenance path your process already uses. The Job View grid exports the job schedule as a workbook with colored cells and a legend sheet, and every report dialog exports to Excel or PDF using the column layout you saved. There is no automated write-back, which keeps your ERP records under your team's control.
</Accordion>
