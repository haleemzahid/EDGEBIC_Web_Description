---
title: "How Capacity Is Computed for a Work Center, Shift, and Day"
description: "The exact formula EDGEBIC uses to turn a shift into available hours: base shift time minus downtime and holidays, scaled by utilization and instances, minus what is already booked and any late-start penalty."
published: 2026-08-03T12:00:00.000Z
modified: 2026-08-03T12:00:00.000Z
category: Scheduling Concepts
author: user-solutions
cluster: edgebic-scheduling-concepts
pillarSlug: edgebic-scheduling-engine-guide
keywords:
  - shift capacity calculation
  - work center capacity
  - machine instances
  - utilization percentage
  - finite capacity scheduling
  - available hours
targetPhrase: "shift capacity calculation"
wordCount: 1550
readingTime: 8
faqQuestions:
  - question: "How does a scheduler calculate a work center's available hours for a day?"
    answer: "EDGEBIC starts from the base shift hours for that weekday, subtracts holiday and downtime hours, multiplies by the utilization percentage, multiplies by the number of machine instances, then subtracts hours already allocated and any late-start penalty. A three-instance center on a seven-hour shift at 80 percent utilization with one hour of downtime and eight hours already booked offers 6.4 available hours."
  - question: "Why does the calculation multiply by instances and utilization separately?"
    answer: "Because they answer different questions. Instances count how many physical machines run the shift, so two identical machines double the hours. Utilization is the fraction of the shift you plan against, deliberately below 100 percent to leave protective slack. Multiplying both means a work center's real offered capacity reflects how many machines exist and how hard you are willing to load them, not just the raw clock time of the shift."
  - question: "What is a late-start penalty in a capacity calculation?"
    answer: "It is the capacity lost when an operation cannot begin until partway through a shift. If a shift runs 08:00 to 16:00 but the operation's earliest start is 10:00, the first two hours of that shift are unusable for it, so EDGEBIC subtracts them. The rest of the shift is available, but the day-one slot is clipped to the hours that remain after the constrained start."
qaQuestions:
  - question: "Our work center has two machines and an eight-hour shift, so why does the schedule think it only has about thirteen hours, not sixteen?"
    answer: "Because raw clock time is not offered capacity. Two machines on an eight-hour shift is sixteen machine-hours only at 100 percent utilization with no downtime. If you run that center at 80 percent utilization to leave protective slack, EDGEBIC scales the sixteen hours down to 12.8, and any scheduled downtime or partial holiday comes off before that multiply. The thirteen-ish hours you see is the honest figure the scheduler will actually fill, which is deliberately below the theoretical maximum so the center is not planned to the edge."
  - question: "A job cannot start until 10:00 and the schedule only gave it six hours on the first day. Where did the other two go?"
    answer: "They went to the late-start clip. The shift runs 08:00 to 16:00, but the operation's earliest start is 10:00, so the 08:00 to 10:00 window cannot be used by this operation and EDGEBIC subtracts it. Six hours remain on day one, which is exactly what the schedule allocated, and the rest of the work carries to the next available shift. This is not lost capacity in general, another job with an earlier start could use that 08:00 to 10:00 window, it is just unavailable to an operation that cannot begin until later."
---

**EDGEBIC by User Solutions computes a work center's available hours for a given shift and day with one formula: take the base shift hours, subtract holiday and downtime, multiply by the utilization percentage, multiply by the number of machine instances, then subtract hours already allocated and any late-start penalty.** The result is the finite capacity bucket the scheduling engine fills, and every term in it corresponds to a real reason the shift is worth more or less than its clock length suggests.

A shift on the calendar says "08:00 to 16:00." That is not the same as how many machine-hours the shift can actually deliver to a schedule. The gap between those two numbers is exactly what this calculation captures, and it is the number that makes finite capacity finite.

## The formula, term by term

For each combination of work center, date, and shift, EDGEBIC builds one bucket:

```
available = (base shift hours - holiday hours - downtime hours)
            x utilization %
            x number of instances
            - already allocated hours
            - late-start penalty
```

Each term earns its place:

- **Base shift hours** is the working length of the shift for that weekday, net of breaks. A shift defined 08:00 to 16:00 with a one-hour break contributes 7 hours.
- **Holiday and downtime** come off the top. A whole-day holiday zeroes the shift; a partial holiday or a scheduled maintenance window reduces it.
- **Utilization percentage** scales the shift to the fraction you are willing to plan against. Running at 80 percent leaves deliberate slack, which is the point of [protective capacity](/blog/protective-vs-productive-capacity).
- **Number of instances** multiplies by how many physical machines run the shift. Two identical machines double the hours.
- **Already allocated** subtracts what earlier operations have already booked in this bucket, so the number is what remains, not the gross.
- **Late-start penalty** clips the first usable slot when an operation cannot begin at the shift's start.

The order matters. Holiday and downtime are subtracted before the multipliers because they shorten the shift itself, while utilization and instances scale the shortened shift. Get the order wrong and the same inputs produce a different, wrong number.

## Why instances and utilization are separate

It is tempting to fold these into one figure, but they answer different questions and behave differently. Instances is a count of physical capacity: how many machines exist and run this shift. Utilization is a policy: how hard you choose to load whatever machines you have. A work center might have three instances but be planned at 75 percent because you want a cushion for variability and breakdowns.

Keeping them separate means you can change one without disturbing the other. Add a fourth machine and instances goes up while your utilization policy holds. Decide to plan tighter and utilization rises while the machine count is untouched. The [hours-based capacity model](/blog/hours-based-vs-piece-based-capacity-modeling) uses both because both are real levers a planner controls.

## The late-start clip

The final term is easy to miss but shapes a lot of "why only six hours?" questions. An operation's earliest start is usually the end of the step before it, and that can land partway through a shift. When it does, the portion of the shift before that start is unusable for this operation, so EDGEBIC subtracts it. A shift from 08:00 to 16:00 with an operation that cannot begin until 10:00 offers this operation only 6 hours on day one, even though the shift's full length is longer.

This is a per-operation clip, not a permanent loss. A different job that can start at 08:00 could still claim that early window. It is specific to the operation whose constrained start clipped it, which is why [the allocator filling a partial first shift](/blog/how-the-allocator-fills-a-partial-first-shift) is its own distinct behavior.

## A worked example: one machine center, one day

Take a milling center on a Monday. The day shift is 08:00 to 16:00 with a one-hour break, so 7 base hours. The center runs three machine instances at 80 percent utilization. Monday has a scheduled 1-hour maintenance downtime and no holiday, and 8 hours are already allocated to earlier jobs.

Work the formula:

| Step | Calculation | Result |
|---|---|---|
| Base shift hours | 8:00 to 16:00 minus 1h break | 7.0 h |
| After holiday | no holiday | 7.0 h |
| After downtime | 7.0 minus 1.0 | 6.0 h |
| After utilization | 6.0 x 0.80 | 4.8 h |
| After instances | 4.8 x 3 | 14.4 h |
| After allocation | 14.4 minus 8.0 | 6.4 h |

So this bucket offers 6.4 available hours. If the next operation to reach this center also had a constrained start of, say, 10:00, a late-start penalty would come off that 6.4 as well. Every figure is traceable to a real cause: the break, the maintenance, the utilization policy, three machines, and the work already on the books.

## Why the calculation is the foundation

This one number is what the rest of the engine trusts. The [multi-shift allocator](/blog/how-multi-shift-allocation-fills-capacity) walks the calendar consuming these buckets in priority order. Holidays and downtime enter here, so they shape the schedule and the dashboards identically. And because the formula is deterministic, the same inputs always yield the same available hours, which is why a schedule is auditable: pick any placement and you can rebuild the capacity it consumed.

There is one important exception to the formula. When a planner enters a capacity override for a specific work center, shift, and date, that override replaces the formula outright, which is how you model an authorized Saturday of overtime or a short-staffed day without disturbing the standing calendar. That mechanism is covered in [how a daily capacity override replaces the formula](/blog/how-a-daily-capacity-override-replaces-the-formula).

For the wider context of why respecting this bucket matters at all, [finite versus infinite capacity scheduling](/blog/finite-vs-infinite-capacity-scheduling) makes the case, and the [complete scheduling engine guide](/blog/edgebic-scheduling-engine-guide) shows where capacity sits in the full pipeline. To see the formula run against your own shifts and machines, explore [the EDGEBIC engine](/edgebic) or [bring your data to a demo](/contact-us).

<Accordion title="How does a scheduler calculate a work center's available hours for a day?">
EDGEBIC starts from the base shift hours for that weekday, subtracts holiday and downtime hours, multiplies by the utilization percentage, multiplies by the number of machine instances, then subtracts hours already allocated and any late-start penalty. A three-instance center on a seven-hour shift at 80 percent utilization with one hour of downtime and eight hours already booked offers 6.4 available hours.
</Accordion>

<Accordion title="Why does the calculation multiply by instances and utilization separately?">
Because they answer different questions. Instances count how many physical machines run the shift, so two identical machines double the hours. Utilization is the fraction of the shift you plan against, deliberately below 100 percent to leave protective slack. Multiplying both means a work center's real offered capacity reflects how many machines exist and how hard you are willing to load them, not just the raw clock time of the shift.
</Accordion>

<Accordion title="What is a late-start penalty in a capacity calculation?">
It is the capacity lost when an operation cannot begin until partway through a shift. If a shift runs 08:00 to 16:00 but the operation's earliest start is 10:00, the first two hours of that shift are unusable for it, so EDGEBIC subtracts them. The rest of the shift is available, but the day-one slot is clipped to the hours that remain after the constrained start.
</Accordion>
