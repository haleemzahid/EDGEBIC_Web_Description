---
title: "How Hours and Pieces Convert on the Shop Floor in EDGEBIC"
description: "EDGEBIC derives one actuals quantity from the other using an effective rate. Learn how hours-primary and pieces-primary work centers convert and why the rate is snapshotted."
published: 2026-08-02T12:00:00.000Z
modified: 2026-08-02T12:00:00.000Z
category: Shop Floor Execution
author: user-solutions
cluster: edgebic-shop-floor
pillarSlug: edgebic-shop-floor-guide
keywords:
  - hours pieces conversion
  - effective rate scheduling
  - pieces per hour
  - actual entry mode
  - shop floor actuals
  - production rate snapshot
targetPhrase: "hours pieces conversion"
wordCount: 1400
readingTime: 7
faqQuestions:
  - question: "How does EDGEBIC convert between hours and pieces?"
    answer: "It uses an effective rate in pieces per hour, computed from the routing step's pieces-per-unit and cycle time, multiplied by the work center's efficiency factor. From that rate, pieces equal hours times rate, and hours equal pieces divided by rate. Which side an operator enters depends on the work center's entry mode; the other side derives automatically so a day always has both a time figure and a piece figure."
  - question: "What is the difference between hours-primary and pieces-primary work centers?"
    answer: "On an hours-primary work center the operator drives an elapsed clock and pieces are derived from hours times the rate. On a pieces-primary work center the operator taps piece counters and hours are derived from pieces divided by the rate. A both mode accepts each independently so variance between them can be measured. The mode simply decides which quantity the operator captures directly and which the system computes."
  - question: "Why is the effective rate snapshotted onto each punch?"
    answer: "The rate is frozen at the moment a punch opens so historical records stay accurate even if the routing is edited later. If someone changes a step's cycle time next month, punches already recorded keep the rate they were captured with, and their derived quantities do not silently shift. Report accuracy over past work does not depend on the current routing staying unchanged."
qaQuestions:
  - question: "Our pieces-primary work center shows zero derived hours. What happened?"
    answer: "The effective rate came out as zero, which happens when the routing step has no cycle time or no pieces-per-unit to compute a rate from. EDGEBIC records a zero rate honestly rather than fabricating a number, so the derived side stays zero. On a pieces-primary work center that means the operator must enter piece counts manually, because there is no rate to convert them into hours. Fix the routing's cycle time and the conversion resumes on new punches."
  - question: "The observed rate on a finished day is lower than the routing standard. Is that a problem?"
    answer: "Usually it is information, not an error. When a day logged both real hours and real pieces, EDGEBIC computes the observed rate as pieces divided by hours for that day, and it can differ from the standard. A run that made 42 pieces in 7.53 hours shows about 5.58 pieces per hour against a 7.14 standard, because a quality hold ate into the time. That gap is exactly the variance you want to see and act on."
---

**EDGEBIC by User Solutions converts between hours and pieces using an effective rate in pieces per hour, so a day's actuals always carry both a time figure and a piece figure even when the operator entered only one.** The rate comes from the routing step and the work center's efficiency, and it is frozen onto each punch when it opens. This bidirectional conversion is what lets an hours-driven machine and a piece-driven machine feed the same [shop floor data collection](/blog/edgebic-shop-floor-guide) model without anyone doing arithmetic by hand.

Some operations are naturally timed and some are naturally counted. A long paint cure is an elapsed clock; a stamping press is a piece count. EDGEBIC does not force one metric on both. It lets each work center capture the quantity that fits, then derives the other so the plan and the reports always have a full picture.

## The Effective Rate

The conversion hinges on one number, the effective rate:

```
effective rate (pieces/hour) = (pieces per unit / cycle time) x work center efficiency factor
```

The pieces-per-unit and cycle time come from the routing step; the efficiency factor comes from the work center. Once you have the rate, both directions follow:

- Pieces = hours x rate
- Hours = pieces / rate

So a step running at 7.14 pieces per hour turns a logged 6 hours into roughly 43 pieces, or a logged 43 pieces into roughly 6 hours. The operator captures one; the system supplies the other.

## Hours-Primary, Pieces-Primary, and Both

Each work center has an entry mode that decides which side the operator drives:

| Mode | Operator captures | System derives |
|---|---|---|
| Hours-primary | Elapsed run clock | Pieces from hours x rate |
| Pieces-primary | Tap-to-increment counters | Hours from pieces / rate |
| Both | Each entered independently | Variance between the two |

Hours-primary suits work where time is the honest metric and a piece count is a byproduct. Pieces-primary suits work where the count is what matters and the time is inferred. Both mode is for operations where you want to capture each directly and measure the gap, which is useful input for performance analysis. The mode is a work center setting, so an operator never has to think about it: they simply tap what the screen asks for.

## Why the Rate Is Frozen at Punch Open

The effective rate is snapshotted onto each punch the moment it opens. This matters because routings change. If a process engineer edits a step's cycle time next month to reflect a faster tool, you do not want last month's completed punches to silently recompute their derived pieces against the new number. Freezing the rate keeps historical records stable: every punch reports the quantities it was actually captured with. Report accuracy over past work no longer depends on the routing staying untouched forever. This is the same immutability principle that governs [actuals across a reschedule](/blog/why-actuals-are-immutable-in-edgebic).

## When the Rate Is Zero

If a routing step has no cycle time or no pieces-per-unit, there is nothing to compute a rate from, and EDGEBIC records the rate as zero rather than inventing one. The consequences are honest:

- On an **hours-primary** work center this is fine. Hours are the primary metric and the derived pieces simply stay at zero until the routing carries a real cycle time.
- On a **pieces-primary** work center it means the piece counter is decoupled from any rate, so the operator enters piece counts manually and no hours are derived from them.

The fix is always the same: give the routing step a real cycle time and pieces-per-unit, and conversion resumes on new punches. Nothing about the frozen historical punches changes.

## The Observed Rate: Standard Versus Reality

There is a second rate worth understanding: the observed rate a day actually ran at. When a day has logged both real hours and real pieces, EDGEBIC computes the observed rate as pieces divided by hours for that day. This is what the day genuinely achieved, and it can differ from the routing standard.

Consider an eight-hour CNC run that produced 42 good pieces in 7.53 recorded hours:

- Standard rate from the routing: about 7.14 pieces per hour.
- Observed rate for the day: 42 / 7.53, about 5.58 pieces per hour.

The gap is not an error; it is the story. A 1.5-hour quality hold ate into the productive time, so the machine made fewer pieces per hour than the standard predicts. That variance is precisely what a planner wants surfaced, because it points at a real event with a documented cause. See how those [piece counts feed the schedule](/blog/how-piece-counts-feed-the-edgebic-schedule) once the day rolls up.

## A Worked Example

Two work centers, one job, different modes.

**Paint-Booth-3 (hours-primary).** The operator runs the elapsed clock across a two-day cure with a mid-job pause. The day's productive hours roll up from run time. Because the mode is hours-primary and no counters were tapped, derived pieces stay at zero unless a rate and count apply, and the planner reads the operation by its hours.

**Press-2 (pieces-primary).** The operator taps a piece counter as parts come off. With an effective rate of 120 pieces per hour, a logged 600 pieces derive to 5 hours. The plan gets both the count the operator captured and the hours the system computed.

Same platform, same rollup, two honest metrics, and neither operator did any conversion math.

## The Bottom Line

Hours and pieces are two views of the same work, and EDGEBIC converts between them with an effective rate drawn from the routing and the work center. The entry mode decides which side an operator captures directly, the other side derives, and the rate is frozen onto each punch so history stays accurate through later routing edits. A zero rate is recorded honestly rather than faked, and the observed rate on a finished day surfaces real variance against the standard. That is how a timed machine and a counted machine feed one clean model for [finite capacity scheduling](/blog/finite-vs-infinite-capacity-scheduling). Explore the whole loop in the [shop floor execution guide](/blog/edgebic-shop-floor-guide), or see the platform at [EDGEBIC](/edgebic).
