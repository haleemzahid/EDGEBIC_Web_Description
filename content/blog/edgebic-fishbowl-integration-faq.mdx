---
title: "EDGEBIC + Fishbowl Integration FAQ: 12 Questions Shops Ask First"
description: "Answers to the Fishbowl scheduling integration questions shops ask first: exports, connectors, routings, multi-location scope, running orders, and how dates get back."
published: 2026-08-02T12:00:00.000Z
modified: 2026-08-02T12:00:00.000Z
category: ERP Integration (EDGEBIC)
author: user-solutions
cluster: edgebic-erp-integration
pillarSlug: edgebic-erp-integration-guide
keywords:
  - Fishbowl scheduling integration questions
  - Fishbowl EDGEBIC FAQ
  - Fishbowl scheduling add-on questions
  - ERP integration import mask FAQ
  - Fishbowl finite capacity scheduling
  - Fishbowl production scheduling integration
targetPhrase: "Fishbowl scheduling integration questions"
wordCount: 1590
readingTime: 8
faqQuestions:
  - question: "Is EDGEBIC a certified Fishbowl connector?"
    answer: "No, and that is a design decision rather than a gap. EDGEBIC integrates through reusable import masks that read Excel, CSV, delimited text, or a database source. You map an export's columns onto EDGEBIC fields once and save it; every run after that is two clicks. Because there is no connector, there is nothing to certify, nothing to version-match, and nothing to repair when either side upgrades."
  - question: "What has to come out of Fishbowl?"
    answer: "Two exports carry the demand and the item master, and a third is optional. Parts (an identifier is the only mandatory column). Open manufacture or work orders (part, quantity, and a date). Optionally, a bill of materials export as a routing scaffold. Any Fishbowl report that saves to Excel or CSV is a valid source, so nobody writes a plugin or touches the database directly."
  - question: "How do finite capacity dates get back into Fishbowl?"
    answer: "Through Excel and whatever date field your manufacture order process already uses. The Job View grid exports the job schedule as a workbook with colored cells and a legend sheet, and every report exports to Excel or PDF with the column layout you saved. There is no automated write-back, which is deliberate: changes to Fishbowl records stay inside your existing process."
  - question: "Can an import disturb orders that are already running?"
    answer: "No, for two independent reasons. Every scheduled job carries a frozen snapshot of the routing it was planned with, so a routing change affects future jobs and leaves work in progress alone. And recorded work is never moved by any run, because an operation with an actual start and end is historical fact. On top of both, imports never schedule anything at all."
qaQuestions:
  - question: "Fishbowl already schedules our manufacture orders. Why add a second scheduling tool?"
    answer: "Because the two schedule different things. Fishbowl sequences manufacture orders by date and material availability, which answers when a job can be released, but it does not model finite machine capacity, so it cannot tell you that three jobs due Friday all need the same press that only runs one at a time. EDGEBIC takes the manufacture orders Fishbowl already holds and places every operation against real shifts, machine counts, and changeover sequences, so the dates it returns are ones the floor can actually hit. You are not replacing Fishbowl's order management; you are adding the capacity dimension it does not have, and feeding the result back as revised dates."
  - question: "We do not record labor hours in Fishbowl. Can we still track progress against the schedule?"
    answer: "Yes. The actuals import is optional, and there are two light ways to feed it. If you capture completion any digital way (a tablet log, a shared sheet, a barcode scan export), map that file through the actuals mask with job number, work center, date, and either hours or pieces, and EDGEBIC derives the missing one from the operation's rate. If you capture nothing today, start by marking operations complete with a date only, which still moves the schedule forward and preserves the recorded work so no reschedule disturbs it. You can add hour detail later without changing anything upstream, because the mask reads whatever columns the file carries."
---

**These are the questions Fishbowl shops ask in the first conversation, answered without hedging: there is no certified connector, the interface is your existing exports, the operation-level routing Fishbowl lacks is built once inside EDGEBIC, and nothing you import can disturb an order already running.** Each answer stands on its own, so skip to whichever one you came for.

EDGEBIC by User Solutions is a finite capacity scheduling platform that sits beside your inventory and order system. The full walkthrough is the [complete Fishbowl integration guide](/blog/edgebic-fishbowl-integration-guide), and the field-level detail lives in the [Fishbowl to EDGEBIC mapping reference](/blog/fishbowl-to-edgebic-data-mapping-reference).

## Is there a certified Fishbowl connector?

No, and it is a design decision rather than a gap. EDGEBIC integrates through reusable import masks that read Excel, CSV, delimited text, or a database source. You map your export's columns onto EDGEBIC's fields once and save the mask; every run after that is two clicks. The same architecture serves Fishbowl, QuickBooks, SAP, and home-grown systems identically, so there is no per-ERP connector to certify, version-match, or repair when either side upgrades. A connector is a dependency. A file is not.

## Does EDGEBIC replace any part of Fishbowl?

No. Fishbowl stays the system of record for parts, inventory, purchasing, and manufacture orders. EDGEBIC reads exports, builds a finite capacity schedule against real shift hours and real machine counts, and hands dates and dispatch lists back as Excel. Inventory transactions and order entry keep running exactly as they do today. The general case for that split is in [why ERP needs a scheduling add-on](/erp-scheduling-add-on), and the head-to-head is on the [RMDB versus Fishbowl page](/compare-products/rmdb-vs-fishbowl).

## Fishbowl schedules manufacture orders already. What does EDGEBIC add?

Finite machine capacity. Fishbowl sequences orders by date and material availability, but it does not model how many identical machines a work center has or how changeover time depends on sequence. EDGEBIC places every operation against those constraints, which is what turns an availability date into a date the floor can hit. The general distinction is in [finite versus infinite capacity scheduling](/blog/finite-vs-infinite-capacity-scheduling).

## Does anything get installed inside Fishbowl?

No. Nothing is installed, no schema is extended, and no interface object joins your upgrade path. The only artifacts on the Fishbowl side are the exports your team builds, which are ordinary reports run by people who already have access to run them.

## Where do work centers and routings come from?

You build them once inside EDGEBIC, because Fishbowl carries parts and a rough bill of materials but not the operation-level detail scheduling needs. Work centers are entered directly or loaded from a small spreadsheet, and routings are drawn in the graphical designer or imported from a routing file, often built by extending a Fishbowl bill of materials export. After that one-time build, every manufacture order for that part reuses its routing automatically.

## How long until the first real schedule?

Most shops see a first finite capacity schedule from their own data in the first working session once one routing exists, and a tuned one inside two weeks. The documented benchmark in the User Solutions lineage is the Plastilite integration: 5 days, Monday to Friday, from first export to a complete optimized schedule with dates synchronized back to the ERP. That figure belongs to that case rather than being a blanket guarantee, but the method is what repeats.

## What happens when Fishbowl is upgraded?

Nothing, in almost every case. The integration reads files rather than calling an interface, so there is no connector version to track and nothing to re-certify. If an upgrade renames a column heading in an export, you re-map that column in the mask once, which takes minutes.

## Our times are per lot, not per piece. Do we rebuild the routings?

No. Put a conversion factor of `0.01` on the hours column in the routing mask, and a cell of 2.5 hours per 100 pieces lands as 0.025 hours per piece. Setup minutes on the same file use `0.016667` on their own column. The factors are saved with the mask, which makes the conversion permanent and auditable through the per-run log.

## Do imported orders schedule themselves?

No, and the separation is deliberate. An import changes data; the scheduler changes the plan. Newly imported orders appear as unscheduled demand and stay there until you run the scheduler. Before the run starts, a confirmation states the scope in plain numbers: how many jobs are new and how many existing jobs are being rescheduled. An import can therefore never silently rearrange a plant. The modes are covered in [EDGEBIC scheduling modes explained](/blog/edgebic-scheduling-modes-explained).

## Can an import disturb orders already running?

No, for two independent reasons. Every scheduled job carries a frozen snapshot of the routing it was planned with, so a routing change affects future jobs and leaves work in progress alone. And recorded work is never moved by any run: an operation with an actual start and an actual end is historical fact. On top of both, imports never schedule anything.

## What if someone runs the same file twice?

Nothing bad happens, on any entity type. Master data comes back Reused (matched and left untouched) unless you explicitly enabled updates. Routing imports wipe and recreate a part's steps rather than appending, so steps never accumulate. Actuals always overwrite the days a file carries, so a double import never double-counts. The import system assumes files get re-run, because in real shops they do.

## What can import never bring across?

The settings that most change what a schedule looks like, because Fishbowl has no source for them. Work center groups (machine pools re-shopped on every reschedule, with per-member efficiency factors). The sequence-dependent setup matrix that lets the optimizer sequence work to cut changeover. Operator skills and rosters. Bottleneck anchoring. Lot streaming with transfer batches. Each is configured once inside EDGEBIC and then applies to every imported order afterwards. The [EDGEBIC product overview](/edgebic) maps them.

## Who runs this day to day?

The planner, in about twenty minutes each morning, without IT. Involve IT exactly once, to turn the Fishbowl exports into saved reports that land in a known folder with stable names. After that the routine is export, run the saved masks, run the scheduler, publish the dispatch lists. For the same questions answered for other platforms, the [QuickBooks](/blog/edgebic-quickbooks-integration-faq) and [Odoo](/blog/edgebic-odoo-integration-faq) FAQs follow the same shape.

## Still deciding?

The fastest way to settle any of this is your own export. Pull this week's open manufacture orders and your part list, then [bring them to a demo](/contact-us): mapping them live takes minutes, and once one routing exists you leave having watched your own shop scheduled against its own capacity.

<Accordion title="Is EDGEBIC a certified Fishbowl connector?">
No, and that is a design decision rather than a gap. EDGEBIC integrates through reusable import masks that read Excel, CSV, delimited text, or a database source. You map an export's columns onto EDGEBIC fields once and save it; every run after that is two clicks. Because there is no connector, there is nothing to certify, nothing to version-match, and nothing to repair when either side upgrades.
</Accordion>

<Accordion title="What has to come out of Fishbowl?">
Two exports carry the demand and the item master, and a third is optional. Parts (an identifier is the only mandatory column). Open manufacture or work orders (part, quantity, and a date). Optionally, a bill of materials export as a routing scaffold. Any Fishbowl report that saves to Excel or CSV is a valid source, so nobody writes a plugin or touches the database directly.
</Accordion>

<Accordion title="How do finite capacity dates get back into Fishbowl?">
Through Excel and whatever date field your manufacture order process already uses. The Job View grid exports the job schedule as a workbook with colored cells and a legend sheet, and every report exports to Excel or PDF with the column layout you saved. There is no automated write-back, which is deliberate: changes to Fishbowl records stay inside your existing process.
</Accordion>

<Accordion title="Can an import disturb orders that are already running?">
No, for two independent reasons. Every scheduled job carries a frozen snapshot of the routing it was planned with, so a routing change affects future jobs and leaves work in progress alone. And recorded work is never moved by any run, because an operation with an actual start and end is historical fact. On top of both, imports never schedule anything at all.
</Accordion>
